# Configurações do Servidor
PORT=3100
NODE_ENV=development

Configurações do MongoDB Atlas
#IMPORTANTE: Configure com suas credenciais REAIS do MongoDB Atlas
# Opção 1: Usar variáveis individuais (descomente e configure)
DB_USER=fa
DB_PASS=Andrade744
DB_NAME=portal-aluno

# Opção 2: Usar URI completa (recomendado - descomente e configure)
# MONGODB_URI=mongodb+srv://fa:<db_password>@cluster0.7hrgleb.mongodb.net/?appName=Cluster0

# Configurações JWT
# IMPORTANTE: Use uma chave forte e única em produção (mínimo 32 caracteres)
JWT_SECRET=senac-portal-aluno-secret-key-2024-mude-esta-chave-em-producao
JWT_EXPIRES_IN=7d

# Configurações CORS (separe múltiplas origens por vírgula)
# Para desenvolvimento local:
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# URL do Frontend (para links em emails)
FRONTEND_URL=http://localhost:5173

